name: CI

on:
  push:
    branches:
      - KAN-3
  pull_request:
    branches:
      - KAN-3

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Set up Python
      uses: actions/setup-python@v2
      with:
        python-version: '3.11'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Initialize database
      run: |
        python /Users/naveenbhansali/git/flights/scripts/init_db.py

    - name: Run tests
      run: |
        pytest
